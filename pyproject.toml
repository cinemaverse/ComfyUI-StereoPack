[project]
name = "comfyui-stereopack"
version = "1.0.0"
description = "Stereo video nodes for ComfyUI: consecutive frames -> side-by-side pairs, eye split/join/swap, and temporal-boil removal."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = []

[project.urls]
Repository = "https://github.com/cinemaverse/ComfyUI-StereoPack"
"Bug reports" = "https://github.com/cinemaverse/ComfyUI-StereoPack/issues"

[tool.comfy]
PublisherId = "cinemaverse"
DisplayName = "StereoPack"
# Icon = ""   # optional: add assets/icon.png and point at it
